# Security

The deployed system's security policy and architecture are documented in [`single-agent/SECURITY.md`](single-agent/SECURITY.md). The multiagent rewrite will define its security boundary when implementation begins.
