[project]
name = "tollchat-pricing-v2"
version = "1.0.0"
requires-python = ">=3.13"
dependencies = [
    "bedrock-agentcore>=1.21.0,<2",
    "boto3[crt]>=1.43.70",
    "defusedxml>=0.7.1",
    "psycopg[binary]>=3.3.4",
    "pydantic>=2.13.4",
    "strands-agents[openai]>=1.52.0",
]

[dependency-groups]
dev = [
    "coverage[toml]>=7.15.4",
    "pyright",
    "pytest",
    "ruff",
    "strands-agents-evals==1.1.0",
    "tiktoken==0.14.0",
]

[tool.pyright]
typeCheckingMode = "strict"
reportMissingTypeStubs = false
exclude = ["**/__pycache__", ".venv", "infra/build", "lambdas/agent_usage_rollup/tests", "lambdas/fetcher/tests", "lambdas/loader/tests", "lambdas/publisher/tests"]

[tool.ruff]
extend-exclude = ["infra/build"]

[tool.ruff.lint]
select = ["ANN", "B", "DTZ", "E4", "E7", "E9", "F", "I", "RUF", "SIM", "UP"]

[tool.ruff.lint.per-file-ignores]
"**/tests/*.py" = ["ANN"]

[tool.pytest.ini_options]
addopts = '-m "not live"'
markers = ["live: hits the network and RDS; run explicitly with -m live"]
pythonpath = ["."]

[tool.coverage.run]
branch = true
source = ["agent_tools"]

[tool.coverage.report]
fail_under = 75
include = ["agent_tools/get_annual_toll_ballpark.py"]
show_missing = true
