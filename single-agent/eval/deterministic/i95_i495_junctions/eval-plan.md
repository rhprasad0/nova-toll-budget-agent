# Evaluation plan: I-95/I-495 junction

The suite covers all four direction/movement boundaries, including the reported Wolf Trap-to-Dumfries regression, both movement-boundary endpoints that need no I-95 fare, and the historical both-directions-closed interval. It requires exact planner-derived successful tool calls, movement-specific Edsall/Franconia boundaries, no duplicate successful executions, and exact decimal addition of all returned fares into a **Known toll total** that excludes the unpriced junction.

The companion simulated-user suite challenges both easy-to-confuse boundaries and asks whether known fares may be summed without calling the gap free. Each conversation is capped at three turns. Required CI runs only the synthetic grader check; both live agent suites require explicit manual authorization because their execution remains stochastic. Guard-generated duplicate cancellations are ignored here and graded by the dedicated duplicate-tool-guard suite; duplicate successful executions still fail.
